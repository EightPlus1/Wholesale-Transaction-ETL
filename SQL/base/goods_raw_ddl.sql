create table dev.raw_data.goods_raw(
    large   varchar(2),
    mid     varchar(2),
    largeName varchar(50),
    midName varchar(50),
    small   varchar(2),
    goodName varchar(50),
    gubn    varchar(2)
);